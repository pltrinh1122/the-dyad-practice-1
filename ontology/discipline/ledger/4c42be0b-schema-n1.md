# Ontology ledger — dyad-steward — schema test #1: DFD maps clean

> Append-only; one file per contributor's schema-tests. contributor =
> `sha256:4c42be0b…f000` (dyad-steward). Each entry = a discipline mapped onto the field-set, testing
> the schema's **completeness + orthogonality** (NOT the discipline's own claim — that's the
> discipline's ledger).

1. **DFD mapped clean (2026-05-31).** Decision-Framing maps onto `{trigger, move, claim, refutation,
   ledger}` with **no leftover and no empty field**. The mapping *tightened the schema*: it forced
   `mechanism`→derived (computable from the move's steps) and `name`→identifier (unfalsifiable), and
   confirmed `move` decomposes into wu-wei-atomic steps (each drop-tested by a DFD failure-mode).
   **Result: survived — completeness + orthogonality held on a real, settled discipline.**

n=1, single-dyad. The schema is **a proposal seeking its second testimonial** — the next discipline
mapped (or a cross-dyad variant) either compounds this ledger or refutes the schema.
